tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Task log" tasks.robot
  SeleniumOrderProcessing:
    robotTaskName: Order robots from RobotSpareBin Industries Inc

condaConfigFile: conda.yaml
artifactsDir: output